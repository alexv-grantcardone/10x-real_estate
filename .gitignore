# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
build/
.next/
out/

# Environment / secrets
.env
.env.local
.env*.local

# Per-machine identity for the shared memory brain (each teammate sets their own:
# `alex` on Alex's machine, `cherlon` on Cherlon's). Never commit — it's per-machine.
.author

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Vercel
.vercel/
