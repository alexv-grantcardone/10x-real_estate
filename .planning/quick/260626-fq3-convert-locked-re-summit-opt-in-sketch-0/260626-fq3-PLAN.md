---
quick_id: 260626-fq3
slug: convert-locked-re-summit-opt-in-sketch-0
description: "Convert locked RE Summit opt-in (sketch 005 V2) to paste-ready GHL block 01-optin.html"
date: 2026-06-26
mode: quick
status: planned
---

# Quick Task 260626-fq3 — RE Summit opt-in → paste-ready GHL block

## Goal
Promote the **locked** RE Summit opt-in design (sketch 005, winner **V2** inline 10-min hold)
into a production, GHL-paste-ready Custom Code block at
`funnels/03-re-summit/steps/01-optin/blocks/01-optin.html`.

## Source of truth
`.planning/sketches/005-summit-optin-10min-hold/index.html` (V2 view).

## Task — build 01-optin.html (single self-contained full-page block)
- **files:** `funnels/03-re-summit/steps/01-optin/blocks/01-optin.html`
- **action:** Transform the sketch into a paste-ready block:
  - Strip sketch scaffolding: `<!doctype>/<html>/<head>/<body>`, variant tab bar, toolbar, `#frame`, `setV()`.
  - Keep **V2 only**: remove the V1 "how it works" band and V3 hero stepper; make the
    `seathold` chips + `cta-reassure` card always-visible (drop the `data-v` conditionals).
  - Scope **every** rule under a unique wrapper id `#gcre-summit-optin` (collision-safe in GHL).
  - Keep Google Fonts link (Inter + Bodoni Moda upright), light/airy tokens, Wistia video hero,
    proof band (count-up), tickets comparison table, what-you'll-learn, Meet Grant, final CTA, footer.
  - **No body styling** (GHL owns `<body>`); background on the wrapper.
  - Script: IIFE + re-entry guard (`window.__gcreSummitOptin`); GHL host-wrapper escape
    (walk parents, clear bg/padding/max-width/width); tier selection; count-up; smooth-scroll.
  - **CTAs:** real `<a>`. Hero/final RSVP + "see options" → in-page `#gcre-tickets`.
    Ticket-section RSVP → `CHECKOUT_URL` config constant, JS appends `?tier=<selected>`.
  - **Image placeholders** with graceful fallbacks: `PASTE_TICKET_VIRTUAL_URL`,
    `PASTE_TICKET_EXECUTIVE_URL`, `PASTE_TICKET_VIP_URL`, `PASTE_GRANT_PHOTO_URL`.
  - **No countdown on Page 1** (ROADMAP); 10-min hold expressed as a stated mechanic only.
  - Comment header: what it is, GHL element, `CHECKOUT_URL` + image setup steps.
- **verify:** file exists; contains `#gcre-summit-optin`, `window.__gcreSummitOptin`,
  `CHECKOUT_URL`, the 3 `PASTE_TICKET_*_URL` placeholders, `PASTE_GRANT_PHOTO_URL`;
  no `data-v=`, no `#nav`/`#tools`/`<!doctype` scaffolding; render-check in Chrome (desktop+mobile).
- **done:** A self-contained block that pastes into one GHL Custom Code element and renders the
  V2 opt-in, with real `<a>` CTAs wired to CHECKOUT_URL and labeled image placeholders.

## Must-haves
- truths: "V2-only (no V1 band / no V3 stepper)"; "scoped under #gcre-summit-optin"; "no Page-1 countdown"; "CTAs are real <a> to CHECKOUT_URL, per-tier ?tier="; "self-contained, no build step".
- artifacts: `funnels/03-re-summit/steps/01-optin/blocks/01-optin.html`.
- key_links: `.planning/sketches/005-summit-optin-10min-hold/index.html`, `funnels/03-re-summit/steps/01-optin/blocks/README.md`.

## Out of scope
- Real ticket prices / checkout URL / Grant photo (placeholders; backfilled later).
- Page 2 (checkout) and Page 3 (thank-you) — separate tasks.
