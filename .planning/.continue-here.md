---
context: default
phase: null
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-06-27T01:39:09Z
---

# RE Live VIP Upsell page — PAUSED · 2026-06-27

> **Heads up:** this is a SEPARATE workstream from the RE Summit funnel handoff in
> `.planning/HANDOFF.json` + `.planning/sketches/.continue-here.md` (Page 3 / sketch 008,
> still pending). Those were intentionally left untouched. This file = the ad-hoc VIP
> upsell copy/headline work done in session "Real Estate Live Training VIP Page".

<current_state>
Editing the **RE Live VIP upsell page**, file:
`/Users/cseecharran/Documents/Claude Projects/Conversion Rate Optimization Specialist/re-live-vip-upsell-checkout.html`
⚠️ This file lives OUTSIDE the git repo (in the "Conversion Rate Optimization Specialist"
folder), so its edits are NOT committed by git. It must be hand-pasted into GHL to go live
(`https://cardonefreetraining.com/upgrade-page`).

All requested copy + headline edits are DONE and verified in Chrome (desktop + mobile).
The only open item is a clarification on the "offer image" replacement (see remaining_work).
</current_state>

<completed_work>
1. **Copy update (exec changes)** — DONE & verified:
   - Top pill now reads: "Congratulations! You are now registered!" (same pill style).
   - Headline now: script/teal "Don't Wait Until July 11th 2026!" + "Get the most value from this live training!".
   - Removed the line "Upgrade to VIP and go inside the room with Grant."
2. **Orphan-word headline fix** — DONE & verified in Chrome desktop (1280/1440) + mobile (375/390):
   - Date wrapped in `.accent` → forced to ONE line at every width (`display:block; white-space:nowrap;` + `clamp(18px,4.4vw,50px)`).
   - Tagline wrapped in `.hl-rest` with `text-wrap:balance` → never orphans.
   - Desktop h1 capped at **52px** so the tagline sits on ONE line (desktop = 2 lines total). Mobile = date line + tagline balanced to 2 lines. No orphans either view.
3. **Standing practice recorded** ("no orphan words in any headline/copy, desktop AND mobile, every page/doc"):
   - Shared knowledge: `knowledge/conversion-playbook.md` → new "Build-quality standards" section. **Committed (0226908) and pushed to main.**
   - Personal memory: `~/.claude/.../memory/no-orphan-headlines.md` + MEMORY.md index.
   - Created missing `.author` file (= `cherlon`).
</completed_work>

<remaining_work>
- **OPEN QUESTION (blocking the last edit):** User asked to replace the "offer image" with
  `https://assets.cdn.filesafe.space/FIWJhGH5AzdaZCNMxvft/media/6a3c1f8807cf451b94a74f2d.jpg`,
  but the product/offer `<img>` at line ~515 is ALREADY that exact URL. Surfaced the discrepancy
  and asked the user whether (a) they meant a *different* new image (need correct URL), or
  (b) the live GHL page just shows an old image (then this file is already correct → paste to update).
  Awaiting their answer.
- **User action:** paste the updated block into the GHL upgrade-page element to push edits live.
</remaining_work>

<decisions_made>
- Desktop headline capped at 52px (down from 62px) to fit the tagline on one line — user confirmed "came out perfectly," accepted the slightly smaller size.
- No-orphan rule made a permanent build-quality standard (verify both viewports before any headline is "done").
- Per-viewport line preference for this page: desktop = tagline 1 line; mobile = tagline 2 balanced lines.
</decisions_made>

<blockers>
- Offer-image clarification pending from user (the provided URL equals the current one). Non-technical; just needs the user's reply.
</blockers>

## Infrastructure State
- Chrome DevTools MCP browser was used for verification (profile lock at
  `~/.cache/chrome-devtools-mcp/chrome-profile` was cleared once to attach). No servers running.

<context>
Work is essentially complete; this is a near-finished ad-hoc edit, not a multi-task build.
The reusable takeaway is the orphan-fix technique now logged in the playbook.
</context>

<next_action>
Start with: read the user's answer to the offer-image question. If they supply a NEW image URL,
replace the `src` at line ~515 of `re-live-vip-upsell-checkout.html` (single img serves both
desktop + mobile) and re-verify in Chrome. If not, confirm the file is already correct and remind
them to paste the block into GHL.
</next_action>
