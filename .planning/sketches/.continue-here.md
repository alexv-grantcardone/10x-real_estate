---
context: sketch
active_sketch: 001-ticket-section
task: 6
total_tasks: 11
status: in_progress
last_updated: 2026-06-25T00:00:00.000Z
---

# RE Summit — "RSVP MY SEAT" funnel design sketches (PAUSED)

<current_state>
Running the GSD sketch flow to design the RE Summit opt-in funnel, section by section, in the light/airy palette. **Sketch 001 (ticket section) is locked.** Paused mid-conversation about the **Page 1 section map** — the user wanted to clarify the proposed 9-section list before we lock it and start designing the next section.
</current_state>

<completed_work>
- **Funnel flow** designed + published as an artifact (RSVP model: no-payment **10-minute** hold → checkout with a **live 10-min countdown** → buy / book recovery). Logic is skin-independent; the diagram itself is still navy. (flow logic updated 2026-06-25; the published navy flow-diagram artifact still shows the old multi-day model and needs re-doing)
- **Page copy** crafted with the installed `copywriting` skill + the marketing survey VOC. **Headline D chosen:** "Stop Watching Other People Get Rich In Real Estate."
- **Sketch 001 — ticket-section** complete: 3 variants, **winner = C (comparison table)**, real physical-ticket art per tier (`assets/ticket-virtual|executive|vip.png`), mobile = stacked ticket cards. Committed.
- **Palette decision: light/airy is the system default** (`themes/default.css`). Navy preserved as `themes/navy.css` alternate. Sketch is fully token-driven → whole system re-skins from `themes/`.
- Brand rules saved to memory: **no word "FREE", no italics.**
</completed_work>

<remaining_work>
- **(NEXT)** Lock the Page 1 section map (9 sections proposed) + confirm section-by-section approach. User was clarifying when paused.
- Design remaining sections as sketches 002+: hero/opt-in · social-proof band · what-you'll-learn · why-RE/stop-worrying · Meet Grant · testimonials · final CTA · footer.
- Page 2 sketch (reservation confirmed: **live 10-min countdown** + single BUY CTA + specialist link; value stack parked — if it lands per Rosa, items visibly decay off the stack over the 10 min, else plain countdown).
- Email/automation spec: **T+0 immediate "10-minute hold active, secure your seat now, not guaranteed after" email → Page 2 checkout**; after the 10 min lapses, a **recovery email SERIES → booking page** (specialist), which loops "just buy" back to checkout.
- Build GHL paste-ready blocks in `funnels/03-re-summit/` via GSD after sketches approved.
- Optional: re-skin flow diagram artifact navy → airy.
</remaining_work>

<decisions_made>
- Headline **D**; light/airy default palette (navy alternate); ticket section = comparison-table **C** with real ticket art.
- Page 2: single primary **BUY** CTA + tiny "talk to a specialist" link; booking is the later recovery; booking page loops "just buy" back to the abandoned checkout.
- Reverse value stack **parked** pending **Rosa**.
- Scarcity (seats/day) **visual-only for now** (no backend per CLAUDE.md §3).
- Build via GSD; repo writes routed through GSD.
- **PIVOT 2026-06-25:** reservation hold shortened from the old multi-day window to **10 minutes** (Ticketmaster-style); Page 2 = **live 10-min countdown**, with a **contingent decaying value stack** (items drop off over the 10 min) if a stack lands per **Rosa**, else a plain countdown. **Email strategy:** T+0 immediate "10-min hold active, secure now, not guaranteed after" → checkout; post-lapse **recovery SERIES → booking page**. The **10-min checkout timer is a REAL Page-2 hold** and does **NOT** conflict with ROADMAP.md:58 (which bans artificial countdowns on the Page-1 opt-in only).
</decisions_made>

<blockers>
- Page 1 section map not locked (user clarifying) — immediate.
- RCI meaning unknown (body topic 5) — ask Alex.
- Reverse value stack content/decay — pending Rosa.
- Seats/day cap must be made real (or kept honestly vague) before launch.
- Paige's official brand palette pending — light/airy is a working direction, not a final lock.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state.
2. `.planning/sketches/MANIFEST.md` — design direction + palette decision + sketch table.
3. `.planning/sketches/001-ticket-section/README.md` + `index.html` — the locked airy reference.
4. Memory: `re-summit-rsvp-funnel`, `re-summit-copy-rules` — funnel model + brand rules.

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Wrong gsd-sdk path | The local `get-shit-done/bin/gsd-tools.cjs` does NOT exist in this repo; calling it fails | advisory | Use the GLOBAL `gsd-sdk` command for all SDK calls (commit, timestamp) |
| Fake mobile preview | The sketch toolbar "Phone" button only narrows the `#frame` container, NOT the viewport, so it does NOT trigger `@media` breakpoints | advisory | Verify mobile by resizing the actual browser viewport (Chrome DevTools `resize_page`), not the toolbar |

## Infrastructure State
- Sketches default to **light/airy** (`themes/default.css`); navy is `themes/navy.css`. Toolbar dropdown switches them.
- Pages exist only as reviewable artifacts/sketches — **nothing written to `funnels/` repo blocks yet.**
- Uncommitted: `D funnels/03-re-summit/steps/01-optin/blocks/_template.html` — pre-existing deletion, NOT from this session; leave or confirm with user.

<context>
Section-by-section is the agreed approach (one sketch per section, 2-3 variants, pick winner, commit). The 9-section map was proposed; the open question is whether to keep the 7 PDF body topics grouped into sections 4 (how-to) & 5 (emotional) or break some into their own bands. The whole design is token-driven so palette is settled and cheap to change later.
</context>

<next_action>
Start with: re-engage the user's clarification on the Page 1 section map (esp. grouping of the 7 PDF topics), lock the map, then begin **sketch 002 = Hero / opt-in section** in the light/airy theme.
</next_action>
